# TouchZoomBoard3 3.0.2 개발 노트

## 기준

- 릴리스 버전: `3.0.2`
- 프레임워크: .NET Framework 4.8 WPF
- 플랫폼: Windows x64
- 외부 런타임 패키지: 없음
- 필기 필터: `adaptive-v8`
- 시각 설정 마이그레이션: `VisualDesignVersion=22`

## 강화유리 패널

- 패널과 헤더는 선택색을 옅게 섞은 단색 저알파 브러시를 사용하며 넓은 영역에 대각선 다단 그라데이션을 두지 않습니다.
- 외곽은 선택색 방향성 굴절, 상·좌측 입사광, 하·우측 음영과 짧은 국부 반사광으로 구성합니다.
- 패널 농도 0%에서는 표면·헤더·외곽 광학 레이어가 모두 완전 투명해집니다.
- 버튼은 별도 접촉 음영과 굴절 테두리를 사용하고 `Effect`와 `LayoutTransform`을 사용하지 않습니다.
- 파스텔 프리셋과 사용자 지정 RGB는 패널·버튼에 함께 적용되며 재실행 후 복원됩니다.

## 필기 처리

- 속도·굵기·확대 배율을 반영한 연속형 지연 제한을 사용합니다.
- 마지막 입력 한 점을 원시 좌표에 강제로 고정하지 않습니다.
- 마지막 패킷에는 SmoothStep 끝단 이징을 적용해 보정량이 한 번에 풀리며 생기던 돌출을 줄입니다.
- WPF `FitToCurve` 완성 후처리는 사용하지 않아 펜을 뗀 뒤 전체 획이 다시 변하지 않습니다.
- 모드·굵기·배율·오버레이 상태가 바뀌면 활성 세션을 폐기하며, 미종료 상태에서 새 StylusDown이 오면 새 획으로 초기화합니다.

## UI 선 굵기

- 일반 글자는 `Normal`, 선택된 메뉴만 `SemiBold`를 사용합니다.
- 아이콘 기본 선은 굵기 계수 0.64, 최소 0.65 DIP를 사용합니다.
- 형광펜 아이콘의 기준 굵기는 1.6, 지우개 아이콘은 1.4를 사용합니다.
- 이동 손잡이는 1.25 DIP, 좌우 화살표는 1.55 DIP로 조정했습니다.
- 아이콘 내부 벡터의 픽셀 스냅을 해제해 125% 등 비정수 DPI에서 과도하게 굵어지는 현상을 줄였습니다.

## 로그 정책

정식판은 사용자 PC에 오류 로그, 필기 진단 로그 또는 사용 기록 파일을 만들지 않습니다. 개발자가 명시적으로 조건부 컴파일 상수를 켠 빌드에서도 진단 메시지는 Visual Studio 출력 창에만 표시하며 파일 I/O를 사용하지 않습니다.

## 변경 시 유지할 조건

- 컨트롤 패널에 불투명 DWM 색상판을 재적용하지 않음
- 패널 농도 0%에서 배경 알파를 추가하지 않음
- 버튼 아이콘에 Blur·DropShadow 효과를 추가하지 않음
- 전자칠판 직접 TouchUp과 지속형 팝업을 유지
- 굵은 펜의 압력 노이즈 억제를 유지
- 기존 레지스트리·뮤텍스·자동 실행 이름을 유지
