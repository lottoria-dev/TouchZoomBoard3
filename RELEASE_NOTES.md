# TouchZoomBoard3 3.0.2

TouchZoomBoard3 3.0.2 정식 패치 릴리스입니다. 3.0.1의 전자칠판 터치 전환 보정과 기존 필기·강화유리 UI를 유지하면서, 패널 테두리를 얇고 부드럽게 조정했습니다.

## 부드러운 패널 테두리

- 여러 외곽 굴절 레이어가 두꺼운 선처럼 겹쳐 보이던 현상 완화
- 바깥 림을 0.62 DIP의 저알파 선으로 낮추고 안쪽 입사광과 하단 음영을 0.32 DIP 안팎으로 축소
- 상·좌측 국부 반사광의 폭과 밝기를 줄여 경계가 배경과 자연스럽게 이어지는 Glassmorphism 표현 적용
- 버튼의 테두리, 아이콘 굵기와 터치 영역은 변경하지 않음

## 3.0.1 터치 전환 보정 유지

- 짧게 눌러 선택하고 길게 눌러 옵션을 여는 버튼의 이동 허용치를 7 DIP에서 일반 터치 버튼과 같은 12 DIP로 조정
- 적외선 전자칠판의 미세한 좌표 흔들림이 직선 도구에서 펜 도구로 전환하는 짧은 탭을 취소하던 현상 완화
- 길게 누르기 시간과 두께 선택 동작은 기존과 동일하게 유지

## 범용 투명 유리 패널

- 패널 전체의 대각선 다단 그라데이션을 균일한 저알파 면으로 교체
- 삼성·LG 등 모니터의 감마, 색심도, 해상도와 주사율 차이에서 단계형 사선이 드러날 가능성 축소
- 균일한 흰 외곽선 대신 선택색의 방향성 외곽 굴절, 상·좌측 내부 굴절대, 하·우측 음영과 짧은 국부 반사광 적용
- 넓은 패널 면은 선택색이 아주 옅게 섞인 단색 저알파로 유지해 강화유리의 두께감과 배경 선명도 동시 확보
- 유리판 농도 0%에서는 패널 면과 외곽이 모두 사라지고 버튼·이동 손잡이만 유지

## 파스텔 굴절광 개인화

- 중성, 하늘, 민트, 라벤더, 로즈, 피치 프리셋 제공
- Windows 색상 팔레트에서 원하는 RGB 색을 직접 선택 가능
- 선택색을 패널의 옅은 표면색, 외곽·내부 굴절대, 국부 반사광과 버튼 재질에 함께 적용
- 프리셋 색의 명도 차이와 채도를 조정해 하늘·민트·라벤더·로즈·피치를 실제 패널에서 쉽게 구분
- 선택한 프리셋 또는 사용자 지정 색을 저장하고 다음 실행 때 복원

## 선명한 돌출 유리 버튼

- 순백색 테두리 대신 위쪽 입사광과 아래쪽 접촉 음영으로 돌출감 표현
- 버튼 전체에 Blur·DropShadow 효과를 걸지 않아 벡터 아이콘 선명도 유지
- 누름 상태에서 버튼 면과 접촉 음영 간격만 바꿔 물리적인 터치 피드백 제공

## 매끄러운 필기 끝단

- 필터 프로필을 `adaptive-v8`로 갱신
- 마지막 한 점을 원시 좌표에 100% 고정하던 처리를 제거해 최대 6~10 DIP 보정량이 한 번에 풀리던 돌출 현상 차단
- 마지막 입력 패킷 전체에서 SmoothStep 곡선으로 보정량 일부만 점진적으로 줄여 곡률과 끝단 반응 유지
- 기존 속도 적응형 필터, 연속형 소프트 지연 제한과 확대 보정은 그대로 유지
- `FitToCurve=true` 완성 후처리를 사용하지 않아 펜을 뗀 뒤 전체 획이 다시 변하는 현상 방지

## 필기 세션 안정성

- 배율·도구 모드·오버레이 상태 전환 때 활성 필터 세션 취소
- `StylusUp`을 받지 못한 상태에서 새 입력이 시작되어도 이전 획과 연결하지 않음
- 새 입력이 시작되면 미종료 상태를 안전하게 초기화

## 가벼운 UI 표현

- 배율 글자, 도형 메뉴, 툴팁 제목의 기본 굵기를 Normal로 조정
- 선택 메뉴만 SemiBold로 제한해 상태 구분은 유지
- 아이콘 선 굵기와 이동 손잡이·좌우 화살표 선을 낮춰 고배율 DPI에서 뭉쳐 보이는 현상 완화
- 벡터 아이콘의 픽셀 스냅을 해제해 125% 등 비정수 DPI에서도 자연스럽게 표현

## 개인정보와 진단

- 오류 로그와 필기 진단 로그를 포함한 파일 기록 기능 제거
- 트레이의 진단 로그 메뉴 제거
- 화면 내용, 문서, 필기 좌표와 사용 통계를 수집하거나 전송하지 않음
- 개발 빌드에서는 필요한 메시지를 Visual Studio 출력 창에서만 확인 가능

## 정식 릴리스 구조

- 공개 문서와 과거 개발 기록을 분리
- 사용자 ZIP에서 내부 구현 기록, 홈페이지 원본, 로그 분석 문서 제외
- GitHub Actions, 배포 스크립트, 홈페이지와 공개 문서를 정식 `3.0.2`로 통일

## 설치

1. `TouchZoomBoard3_3.0.2_Windows_x64.zip`을 내려받습니다.
2. 원하는 폴더에 완전히 압축 해제합니다.
3. `TouchZoomBoard3.exe`를 실행합니다.

Windows 10 22H2 또는 Windows 11 x64와 .NET Framework 4.8 이상이 필요합니다. 디지털 서명이 포함되지 않아 Windows SmartScreen 안내가 표시될 수 있습니다. 공식 GitHub Release의 ZIP과 SHA-256 파일을 함께 확인하십시오.
